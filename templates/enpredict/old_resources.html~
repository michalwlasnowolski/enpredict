{% extends "enpredict/base.html" %}
{% load staticfiles %}
{% block title %} Resources {% endblock %}
{% block content %}


<div class="container">
<div class="row">
<div class="col-lg-12">
<h2>Resources - {{database_name}}</h2><br>

<!--<form>
<input type="checkbox" name="list" value="audio"/> Audio<br />
<input type="checkbox" name="list" value="video"/> Video<br />
<input type="submit" value="submit" /> 
</form>-->

</div>
</div>
<div class="row">
<div class="col-lg-6">

<form action="download_wigfile" method="post">{% csrf_token %}
  <table class="table table-hover table-striped">
    <thead>
      <tr>
        <th>Chromosome</th>
		{% if database_name != 'Drosophila melanogaster (dm3)' %}
        <th>Specific tissue</th>
		{% endif %}
        <th>Modification</th>
		{% if database_name == 'Drosophila melanogaster (dm3)' %}
        <th>k-mers</th>
		{% endif %}
        <th>View in UCSC</th>
        <th>Download</th>
      </tr>
    </thead>
    <tbody>
{% for object in records %}
      <tr>
        <td>{{object.chr_name}}</td>
		{% if object.species == 'Hs' %}
        <td>{{object.get_specific_tissue_display}}</td>
		{% endif %}
        <td>{{object.get_modification_display}}</td>
		{% if object.species == 'Dm' %}
        <td>{{object.get_kmers_display}}</td>
		{% endif %}
        <td align="center"><a href="http://genome.ucsc.edu/cgi-bin/hgTracks?db={% if object.species == 'Hs' %}hg19{%else%}dm3{% endif %}&position={{object.chr_name}}&hgt.customText=http://regulomics.mimuw.edu.pl/~enpredict/{{object.file_path}}" target="_blank"><span class="glyphicon glyphicon-upload"></span></a></td>
        <!--<td><a href="{% static 'enpredict/data/{{object.file_static_path}}' %}" download><span class="glyphicon glyphicon-download"></span></a></td>-->
<!--<td align="center"><a href="{% static object.file_static_path %}" download><span class="glyphicon glyphicon-download"></span></a></td>-->

<td align="center"><input type="checkbox" class="chkbox" name="check_download" id="checkbox_download" value="{% static object.file_static_path %}"></td>
{% endfor %}
<input type="submit" value="Download">
</form>
</div></div>

    </tbody>
  </table>

<!--<button type="button" id="download_wigfile" class="btn-default" onclick="downloadWigfile('{{ csrf_token }}')">download</button>-->


{% endblock content %}
