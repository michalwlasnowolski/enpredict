import sys

class SeqCutter():

    def make_string(self, fasta):

            sequence=''
            for i in xrange(len(fasta)):
                    seq=fasta[i].strip()
                    sequence+=seq
            return sequence


	
    '''def cutter(self, seq, frame, step):

        num_steps=len(seq)/int(step)
        cut_fasta=''
        

        for i in xrange(num_steps):
            position=i*int(step)
            second_line=seq[position:position+int(frame)]
            cut_fasta+=second_line

            if i<num_steps: cut_fasta+='\n'
        return cut_fasta'''
